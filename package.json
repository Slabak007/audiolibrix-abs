{
  "name": "audiolibrix-provider",
  "version": "1.0.0",
  "description": "Audiolibrix provider",
  "main": "server.js",
  "scripts": {
    "build": "esbuild server.js --platform=node --bundle --outdir=dist"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "cheerio": "^1.2.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "p-limit": "^7.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
